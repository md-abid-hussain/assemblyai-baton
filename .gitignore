node_modules/
.next/
dist/
build/
out/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vercel/
.turbo/
spikes/.cache/
spikes/out/

# Recording kit: private call audio + sidecars (voices stay private until consent is confirmed)
data/calls/*
!data/calls/README.md
tools/recording-kit/participants.json

# Public repo hygiene (DESIGN 3.1): research notes profile judges and hold win estimates.
# Only the live-verified API results (research/10*.md) are published.
research/*
!research/10*.md

# Build bundle (scripts/assemble-bundle.mjs)
bundle/

# Next / test artefacts (WP0b)
next-env.d.ts
*.tsbuildinfo
playwright-report/
test-results/

# Per-work-package git worktrees for parallel agents
.wt/

# Local QA screenshots/reports (shared with the user, never committed)
qa-artifacts/

# Devtools output (WP23): self-hosted Monaco, and the CLI/SDK tarballs served from the app.
# `public/schemas/` stays committed.
public/vendor/monaco/
public/cli/
public/sdk/

# Orchestrator handover notes (local only)
.handover/
