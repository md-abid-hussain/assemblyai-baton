# Zerops: excluded from deployFiles (gitignore syntax). We deploy only ./bundle, but keep this as a safety net
# mirroring .gitignore plus local-only material (DESIGN §8.1, research/16 §pitfalls 4).
.env
.env.*
!.env.example
node_modules/
.next/cache/
coverage/
*.log
spikes/
tools/
research/
data/calls/
tests/
docs/
.github/
.vercel/
.turbo/
bundle/**/.env
bundle/**/.env.*
