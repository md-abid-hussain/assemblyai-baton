# G2 notes: Baton slice integration (WP12·1, D1 Fri Sep 25, ≈15:35–16:30 IST)

**Status: the merge and the checks are green on `main`. The deploy did NOT run — `zcli push` and every request to the
deployed URL were denied by the session's permission system ("Production Deploy").** So the G2 exit criteria in
TASKS-v2 §3.1 (deployed; the live Baton slice on Zerops) are **not met**, and the live G2 check did not run. Nothing
live was called: **$0 AssemblyAI, $0 OpenAI, $0 Polar** this unit.

## 1. Merges (all `--no-ff`, in the planned order)

| Commit | Merge | Conflicts | Notes |
|---|---|---|---|
| `ecb652d` | `wp/wp12` | none | ipKey fix + `/api/status` probe, T-D1-3 part B script, the esbuild override |
| `65b191a` | `wp/wp5` | none | takeover service/machine/controller; already contained post-G1 `main` |
| `557c710` | `wp/wp6` | none | tools, payments, Polar, MockPhone, `/pay/*` |
| `8584be5` | `wp/wp7` | none | Baton console, G2 slice UI; already contained `wp/wp5` |
| `90a9d2f` | `wp/wp9` | none | takes pipeline, scenario kit, STT/extraction caches |
| `b75ca2a` | `wp/wp18` | none | P-1/P-2/P-3 probe script, the tightened F6 VA audit |
| `c913c63` | `wp/wp13` | none | positioning v2.1, `src/content/**`, lablab copy |
| `cb912fb` | `wp/wp7` (notes follow-up) | none | `wp/wp7` had advanced by one notes-only commit (`ca7d2ca`) after its merge |

- **No conflicts anywhere**, so no ownership ruling (TASKS-v2 §4) had to be applied and `package.json` /
  `package-lock.json` never conflicted. **`npm install` was therefore not needed and was not run.**
- **Not merged (G3, per the task):** `wp/wp14a`, `wp/wp14b`, `wp/wp16`, `wp/wp17`. `git branch --no-merged main` lists
  exactly those four.
- `wp/deploy`, `wp/wp1`–`wp/wp4`, `wp/wp5b`, `wp/wp7b`, `wp/wp8` were already in `main` from G1.

## 2. Integration fixes (`0694102`)

Nothing was broken by the merge itself: typecheck, tests and build were green on the merged tree before these. All
three are gaps the branch notes had handed to the integrator.

1. **`[WIRE-CALLS]` was never wired.** `src/server/runs/calls.ts` looks the WP9 call manifest up through a registry, and
   nothing called `registerCallLookup`. `getCallEntry` therefore returned `null` for every call, and
   `src/app/api/stt/token/route.ts` and `src/server/runs/run-service.ts` silently fell back to
   `DEFAULT_CALL_DURATION_MS` (5 min) — losing the Express start (`EXPRESS_LEAD_MS` before the decision point), the
   handoff line and the call's audio format for the streaming params. Wired at boot in `src/instrumentation.ts`:
   `registerCallLookup((id) => getCaseDataSource().getCall(id))`, inside a try/catch that logs to `component:"boot"`.
   Test: `tests/unit/platform/instrumentation.test.ts`.
   (`src/app/call/call-entry.ts` reads `src/generated/calls.json` from disk on its own, so the page was unaffected. The
   bundle does ship `bundle/src/generated/`, traced by Next's standalone output — checked in `bundle/` after `npm run build`.)
2. **`DISCLOSURE_TAX_SUFFIX`** added to `src/server/env.ts` and `.env.example`, `flag01("0")` (off), per
   `requests/wp6-to-integrator.md` item 1. `src/server/tools/wiring.ts` still reads `process.env` directly; the value
   and the default are the same either way, so nothing else changed.
3. **A deploy-time config probe** (§4 below): `src/server/health/config-probe.ts` and `GET /api/health?probe=config`.

No test was weakened, skipped or relaxed. The only skipped test in the suite is the pre-existing one.

## 3. Checks on `main` at `0694102`

| Check | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm test` | **1298 passed, 1 skipped (104 files)**, twice in a row (8.7 s / 11.4 s) |
| `npm run build` | OK. `next build`, `bundle:scripts`, then `[assemble-bundle] bundle/ ready: 1761 files, 52.5 MiB, 0 symlink(s) materialized` |
| conflict markers in the tree | none |

The "0 symlink(s) materialized" line is expected on Windows: the Turbopack `serverExternalPackages` aliases are only
symlinks in the Linux build container, which is where `scripts/assemble-bundle.mjs` has to fix them (deploy.md fix 5).

## 4. The config probe (how to answer "are the secrets set?" without reading a value)

zcli v1.1.2 cannot read a service's env, and reading it over SSH was already refused at WP12·0. So the check is a
route instead:

```
GET https://app-2b25-3000.prg1.zerops.app/api/health?probe=config
→ { ok, db, version, config: { ok: bool, missing: [NAME…], missingOptional: [NAME…] } }
```

- `src/server/health/config-probe.ts` builds it from `missingEnv()`, which compares against `undefined`/`null` and
  returns **names only**. A blank or whitespace value counts as unset (`readRaw` trims and drops empty strings), which
  is what a half-filled Zerops secret field looks like. A test asserts no value can appear in the output.
- `REQUIRED_SECRET_NAMES` (the live Baton slice cannot run without these; deploy.md "Secrets" step 1):
  `ASSEMBLYAI_API_KEY`, `OPENAI_API_KEY`, `CASE_TOKEN_SECRET`, `VISITOR_SECRET`, `ADMIN_KEY`, `CRON_SECRET`,
  `AAI_WEBHOOK_SECRET`, `AGENT_TOOL_SECRET`, `LIMITS_AUTHORITY_KEY`.
- `OPTIONAL_SECRET_NAMES` (Polar step 3; without them payments stay in mock/Simulate mode, which is the G2 criterion):
  `POLAR_ACCESS_TOKEN`, `POLAR_WEBHOOK_SECRET`, `POLAR_PRODUCT_ID`.
- It is **opt-in** (`?probe=config`), so the default `/api/health` body and the Zerops readiness check are unchanged.

## 5. Blocked: the deploy, the health check and the live G2 run

Both of these were denied by the session's permission system, with the reason **"Production Deploy"**:

```
zcli push -P KZNwFJZjSFAVRky0p94BQA -S vUDumVp2Sg25qXBRHKmyxA --setup app \
  --workspace-state clean --version-name g2-0694102
curl https://app-2b25-3000.prg1.zerops.app/api/health
```

The denial was not worked around. Consequences:

- **The G2 build is not deployed.** Zerops still serves the G0 build (`893e31e`, deploy.md).
- **Whether the app secrets are set on Zerops is still unknown.** The probe that would answer it only exists in this
  undeployed build, and every other read of the deployment (env over SSH at WP12·0, a request to the URL here) has been
  refused. deploy.md's last recorded state is "**App secrets are not set on Zerops**" (user action, GUI), and nothing
  since then says otherwise — so the working assumption is still **unconfigured**, but it is an assumption, not a check.
- **The live G2 check (s01 Express → Pass → greeting → confirm → disclose → pay → close → "✓ Verified from
  recording") did not run**, and could not have run even with the deploy (§6, first item).

**What the owner needs to do**, in order:

1. Run the `zcli push` above from `C:/Users/abid1/Desktop/assembly-ai` (the tree is committed; `--workspace-state clean`
   pushes HEAD exactly and ignores the two untracked `docs/*.md` files).
2. `curl -s "https://app-2b25-3000.prg1.zerops.app/api/health?probe=config"` and read `config.missing`.
3. Set whatever it names in the GUI (deploy.md "Secrets" step 1) and restart the `app` service.
4. Then the P-0 probe, which has been waiting since WP12·0 and needs the G2 build:
   ```
   curl -s -H "x-ipkey-probe: 1" -H "X-Forwarded-For: 203.0.113.7" -H "X-Real-IP: 203.0.113.9" \
     https://app-2b25-3000.prg1.zerops.app/api/status >/dev/null
   zcli service log -P KZNwFJZjSFAVRky0p94BQA -S vUDumVp2Sg25qXBRHKmyxA --format SHORT --limit 50 | grep ipkey_probe
   ```
   Expected `keyedOn:"x-real-ip"`, `realIpEqualsXffRight:true`. Otherwise set `IPKEY_MODE` in the GUI (wp12.md).

## 6. Open issues for K-B (D2 10:00)

1. **There is no call asset at all, so `/call/<s01>` cannot run — this blocks the live G2 check independently of the
   secrets.** `data/calls/` holds only `README.md`, `src/generated/calls.json` has **0 calls**, and neither
   `public/calls/` nor `public/data/cached-turns/` exists. The D1 10:00–14:30 recording has not reached this checkout.
   The G2 page shows the unknown-call card until `npm run calls:build` runs over real sidecars (wp9.md "Known gaps").
   The fallback of using "the best available call asset" was not available either: no synthetic take is committed, and
   generating one would put WP9-owned generated assets on `main` on the integrator's authority. That is a decision for
   the planner, not a G2 fix.
2. **WP18's tightened F6 audit ships in this build.** Once it is live, *any* running VA session on the AssemblyAI
   account that is not registered with the Zerops limits authority trips `replay_only` — including agents' laptop
   sessions under the local file guard. Before or at that deploy, every agent running live VA scripts must switch to
   `LIMITS_ROLE=remote` with `LIMITS_AUTHORITY_URL` + `LIMITS_AUTHORITY_KEY` (wp18.md item 1, deploy.md "Guidance for
   local agents"). That switch also needs `LIMITS_AUTHORITY_KEY` set on Zerops, i.e. it depends on §5 step 3.
3. **T-D1-3 part B is PASS** (WP12·0, live, $0.0033): auth is checked only at the WebSocket upgrade, so route #10's
   **10 s VA token window stays**. `g1.md`'s row still reads "not run" — this note supersedes it.
4. **The shared `node_modules` still holds the nested `esbuild@0.18.20`.** The override and the lockfile are correct
   and Zerops's `npm ci` picks the fix up; the laptop copy is a junction shared by every worktree, so `npm ci` was not
   run here while WP14a is still working (wp12.md, optional step 5).
5. **Polar (user, GUI, D2):** embedding allowlist, the webhook and `POLAR_*` / `PAYMENTS_MODE=polar` are still open
   (wp6.md). Until then payments stay in mock mode, which is what the G2 criterion uses.
6. Not re-checked here: the `/dev/csp` probe and the browser matrix against the deployed URL (both need the deploy).

## 7. Worktree sync

`git merge --no-edit main` ran in every `.wt/<wp>` **except `wp14a`** (still working, skipped by instruction). Every
worktree was clean beforehand (`git status --porcelain` empty in all 18) and **every merge succeeded — no conflicts,
nothing aborted**.

| Worktrees | Result |
|---|---|
| `deploy`, `wp1`, `wp2`, `wp3`, `wp4`, `wp5`, `wp5b`, `wp6`, `wp7`, `wp7b`, `wp8`, `wp9`, `wp12`, `wp13`, `wp18` | fast-forward to `0694102` (their own work is already in `main`) |
| `wp14b`, `wp17` | merge commit (their G3 work is ahead of `main`) |
| `wp16` | merge commit; `package-lock.json` auto-merged (WP16's `ipaddr.js` line vs. WP12's esbuild override — both kept, no conflict) |

A second pass after this note was committed fast-forwarded each of them by one commit.

Note for the three G3 worktrees: `main` now carries the tightened F6 audit and the `[WIRE-CALLS]` boot registration, so
`getCallEntry` returns real manifest entries in a dev server for the first time (still `null` for every id while the
manifest is empty — §6 item 1).
