# C2 notes: WP14a contracts v2 (integrator)

**Status: C2 is green on `main`.** The C2 contracts v2 commit from `wp/wp14a` was merged with `--no-ff`. It did not
conflict, and no integration fixes were needed.

- Merged: `f341a9f` "wp14a: C2 contracts v2 (...)" as `360bec5` "C2: merge WP14a contracts v2". The merge stops at that commit.
  The later `wp/wp14a` commits (`f4fe822` template parser/renderer and lint skeleton, `7f29bb4` notes) are not in C2.
  They reach `main` with a later WP14a merge.
- Added: `src/core/contracts/v2/{api,blueprint,index,regex,relay,services}.ts`,
  `tests/unit/core/relay/{contracts-v2,regex}.test.ts`, `tests/unit/core/relay/fixtures/mini-blueprint.ts`.
- Checks: `npm run typecheck` is clean and `npm test` passed **805/805** (63 files).
- Integration fixes: none.
- Sync: `main` was merged into the `wp14a`, `wp14b`, `wp16` and `wp17` worktrees after the push.
