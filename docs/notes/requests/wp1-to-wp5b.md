# WP1 → WP5b: T-D1-0 inputs from the real compiler

1. **Fixtures.** The compiler's exact first `session.update` messages are committed on `wp/wp1`:
   - `tests/unit/core/compiler/__fixtures__/first-update-{confirm,disclose}.{s01,s02}.json`;
   - `VA_KEYTERMS=0`, `deployId` `dev-wp1`.

   They are regenerated by `tests/unit/core/compiler/first-update.test.ts` (`toMatchFileSnapshot`). Please re-run
   T-D1-0 with them at G1 (TASKS §8, D1 13:00 row).
2. **Shape check.** Once your hand-written `scripts/day1/fixtures/first-update-{confirm,disclose}.json` are merged,
   the same test compares their shape with the compiler's output: keys, value types and tool names. Keep those file
   names, or tell me the new ones.
3. **If T-D1-0 fails**, send me the failing field. WP1 then removes it from the whitelist in
   `src/core/compiler/first-update.ts`, as DESIGN App. B says, and the fixtures update in the same commit.
   - `validateFirstUpdate` allows `input.keyterms` only when you pass `{ keytermsEnabled: true }`.
   - `SYSTEM_PROMPT_MAX_CHARS = 8000` is an unmeasured bound; the compiled prompts are about 3.5k chars. Please record
     the largest prompt the live API accepted.
4. **Hold and wrap-up helpers.** These are pure and optional. Use them if you don't want to re-implement §5.8/§5.9.5
   timing. All are in `src/core/compiler/stages.ts` and unit-tested:
   - `advanceHoldDeadline` and `holdTimedOut` (60 s, then +30 s steps in the listed phone states, ≤ 180 s);
   - `reassuranceDue`;
   - `wrapUpDue` (effective cap − 20 s; never in paying or closing);
   - `absoluteCeilingReached`;
   - `onPaymentSucceeded` (the late-success push path).
