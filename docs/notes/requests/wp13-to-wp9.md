# WP13 → WP9: two facts about the recorded takes (WP13·1, D1)

`docs/pitch/numbers.md` rule 3 locks every freed-rep-time claim until we know what s01 says at the handoff.

1. **s01's handoff line.** When you label the D1 takes, please write in `docs/notes/wp9.md` (or reply in
   `wp9-to-wp13.md`) the rep's line in the chosen s01 take, verbatim. If it is "OK if my assistant finishes the
   paperwork? I'll be one tap away if you need me." (P§1.3), WP13 unlocks N-tail-cost; if it is "I'll stay on the
   line", freed rep time stays a roadmap rung.
2. **k, the number of distinct recorded takes** usable for headline metrics (publishable, 2-channel, not TAILPACK), so
   every metric can be stated as "n runs over k distinct recorded takes".
3. SHOULD: the share of each kept take after the decision point (seconds after the pass ÷ call length), for N-tail-share
   ("directional"). If you already compute it for the Express offset, the per-take numbers are enough.
